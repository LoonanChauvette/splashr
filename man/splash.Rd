% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splash.R
\name{splash}
\alias{splash}
\title{Convert Splash colors to hex strings}
\usage{
splash(color, theme = "default")
}
\arguments{
\item{color}{A numeric or character vector of Splash colors. See \emph{Input
grammar} above.}

\item{theme}{Theme name (a single string). Defaults to \code{"default"}. Use
\code{\link[=splash_themes]{splash_themes()}} to list available themes and \code{\link[=splash_theme]{splash_theme()}} to
define your own.}
}
\value{
A character vector of \verb{#RRGGBB} hex strings (lowercase). Length
equals the number of resolved colors: 1 per full code or name, 10 per
wildcard.
}
\description{
\code{splash()} is the main entry point of the package. It converts Splash
color codes to \verb{#RRGGBB} hex strings that can be used directly in plots
and graphics.
}
\details{
A Splash color is a 3-digit number \code{RGB} where each digit is a channel
from 0 to 9 (red, green, blue in that order). \code{000} is black, \code{999} is
white, \code{900} is red, \code{090} is green, \code{009} is blue.
}
\section{Input grammar}{


\code{splash()} accepts a vector of mixed inputs, each resolved independently:

\itemize{
\item \strong{Numeric} -- an integer code, zero-padded to 3 digits.
\code{splash(900)} == \code{splash("900")}. Codes outside 0-999 are an error.
\item \strong{3-digit code string} -- a string of exactly three digits.
\code{splash("407")}.
\item \strong{Wildcard string} -- a 3-character string containing exactly one
\verb{_}, e.g. \code{"9_9"}. Expands to the 10 native Splash colors along the
wildcard axis (the digit varies 0 to 9), so \code{"9_9"} yields 10 hex
strings: \code{"909"}, \code{"919"}, ..., \code{"999"}.
\item \strong{Theme color name} -- a string matching a named color in the
active theme, e.g. \code{"green"}. Names are theme-aware: \code{"green"}
resolves to \code{"090"} in the default theme but \code{"093"} in cellpond.
}

Mixing is fine: \code{splash(c("green", "9_9", 900))} returns 1 + 10 + 1 = 12
hex strings.
}

\examples{
splash("900")
splash(c("900", "090", "009"))
splash(900)

# By name (theme-aware)
splash("green")
splash("green", theme = "cellpond")

# A 10-step ramp along one channel ("_" is a wildcard)
splash("9_9")

}
