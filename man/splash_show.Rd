% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splash_show.R
\name{splash_show}
\alias{splash_show}
\title{Display Splash colors as a swatch plot}
\usage{
splash_show(color, theme = "default", labels = TRUE, ...)
}
\arguments{
\item{color}{A Splash color specification passed to \code{\link[=splash]{splash()}}. A single
value or a vector.}

\item{theme}{Theme name (a single string). Defaults to \code{"default"}.}

\item{labels}{Logical. Whether to draw text labels (codes) on the swatches.
Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[graphics:par]{graphics::par()}} (e.g. \code{mar}).}
}
\value{
Invisibly, a character vector of the hex colors drawn (the same
value \code{\link[=splash]{splash()}} would return for \code{color}).
}
\description{
\code{splash_show()} draws a quick horizontal swatch strip of one or more Splash
colors, labeling each with its code. It uses base R graphics only (no
ggplot2), so it works anywhere R runs. It's handy for exploring colors and
palettes, and for the package README.
}
\details{
The function accepts anything \code{\link[=splash]{splash()}} accepts: a 3-digit code, a
wildcard ramp (e.g. \code{"9_9"}), a theme color name, a numeric code, or a
vector mixing any of these. Labels show the resolved code for each swatch;
for theme names the name is shown in parentheses.
}
\examples{
\donttest{
splash_show("900")
splash_show(c("red", "green", "blue"))
splash_show("9_9")                 # a 10-step ramp
splash_show(splash_palette(4))
splash_show("9_9", theme = "cellpond")
}

}
