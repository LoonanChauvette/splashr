% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splash_named.R
\name{splash_named}
\alias{splash_named}
\title{Named Splash colors for a theme}
\usage{
splash_named(theme = "default")
}
\arguments{
\item{theme}{Theme name (a single string). Defaults to \code{"default"}.}
}
\value{
A named character vector of \verb{#RRGGBB} hex strings, in the theme's
authored order. Length equals the number of named colors in the theme
(8 for the bundled themes).
}
\description{
Returns the theme's named colors as a named character vector of hex
strings. Useful on its own (e.g. \code{splash_named()["green"]}) or as the
backbone for \code{\link[=splash_palette]{splash_palette()}}.
}
\details{
Names are part of the theme: the same name maps to different hex values in
different themes. For example, \code{"green"} is \verb{#00ff00} in the default theme
but \verb{#17ff80} in cellpond.
}
\examples{
splash_named()
splash_named("cellpond")

# Pick one by name
splash_named()["green"]

}
