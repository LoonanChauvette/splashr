% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splash_palette.R
\name{splash_palette}
\alias{splash_palette}
\title{Generate an n-color Splash palette}
\usage{
splash_palette(n = 8L, theme = "default")
}
\arguments{
\item{n}{Integer. Number of colors requested. Defaults to \code{8}. Values
larger than the theme's backbone are capped (with a warning); values
less than 1 are an error.}

\item{theme}{Theme name (a single string). Defaults to \code{"default"}.}
}
\value{
A character vector of \verb{#RRGGBB} hex strings, length \code{min(n, backbone_size)}.
}
\description{
\code{splash_palette()} returns \code{n} hex colors sampled from the theme's named
color backbone. It is the quickest way to get a ready-to-use categorical
palette for plotting.
}
\details{
Selection is evenly spaced across the backbone in its authored order,
always preserving that order. For \code{n} less than the backbone size, every
\code{floor(size / n)}-th color is taken (with the first and last always
included). For \code{n} equal to the backbone size, the full backbone is
returned in order.

Because the v0.1 palette is restricted to the theme's named backbone, \code{n}
is capped at the backbone size (8 for the bundled themes). Requesting more
returns the full backbone with a warning. Interpolated palettes for larger
\code{n} are planned for a future release.
}
\examples{
splash_palette()          # the full default backbone (8 colors)
splash_palette(4)         # 4 evenly-spaced colors
splash_palette(3, theme = "cellpond")

}
