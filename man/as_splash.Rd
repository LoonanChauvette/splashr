% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_splash.R
\name{as_splash}
\alias{as_splash}
\title{Convert hex colors to Splash codes}
\usage{
as_splash(hex, theme = "default")
}
\arguments{
\item{hex}{A character vector of hex colors. A leading \verb{#} is optional;
shorthand \verb{#RGB} (e.g. \code{"#fff"}) is expanded to \verb{#RRGGBB}. Case is
ignored.}

\item{theme}{Theme name (a single string). Defaults to \code{"default"}.}
}
\value{
A character vector of 3-digit Splash code strings (e.g. \code{"900"}),
the same length as \code{hex}.
}
\description{
\code{as_splash()} is the inverse of \code{\link[=splash]{splash()}}: it takes \verb{#RRGGBB} hex strings
and returns the nearest Splash code for the given theme. "Nearest" means
each channel is independently matched to the closest value in the theme's
0-9 lookup table.
}
\details{
Because the mapping is theme-specific, round-trips are stable within a
theme: \code{as_splash(splash(code, theme), theme)} returns \code{code} for every
valid Splash code.
}
\examples{
as_splash("#ff0000")
as_splash("00ff00")
as_splash(c("#ff0000", "#00ff00", "#0000ff"))

# Round-trips are stable within a theme
code <- "407"
as_splash(splash(code), theme = "default")

# Theme-aware: the same hex may map to different codes
as_splash("#171d28", theme = "cellpond")

}
